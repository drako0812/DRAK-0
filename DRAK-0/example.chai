trace("example.chai");
trace("Starting Up...");

def update() {
  for(var j = 0; j < 240; ++j) {
    for(var i = 0; i < 320; ++i) {
      var c = random(0, 64);
      pix(i, j, c);
    }
  }
}

-- potential optimization

-- def update() {
--     FOR2("j", 0, 240, 1, "i", 0, 320, 1, fun(j, i) {
--         var c = random(0, 64);
--         pix(i, j, c);
--     });
-- }
